# Raw seeds — problem-solving

Re-fetch (2026-08-22) of the ten cited seed pages after the original warm
snapshots (`/tmp/digest-sources/ps`) were lost in the WSL restart of the same
day. Sources are the external links of `reference/problem-solving.html`
(Wikipedia canon one-for-one with goal.md's seed list).

These are Track A distillation inputs: source keys will be carved from them
into `reference/sources/<key>.md` per `_TEMPLATE.md`; the raw pages stay here
so a future snapshot loss cannot stall the pipeline again.

Fetched live via scrapling light mode; sizes 21–155 KB each; all exit 0.
